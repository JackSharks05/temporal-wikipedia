crawler that doesn't store full revisions just gets year end state which is fine for many of our indexes
also doesn't rely on map reduce, just has an orchestrator holding all state making everything much easier and faster.
