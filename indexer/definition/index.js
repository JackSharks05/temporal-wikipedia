const MAPPER_MODULE = require.resolve('./mapper');
const REDUCER_MODULE = require.resolve('./reducer');
const {listArticleKeys} = require("../temporalDiff/index")


function buildDistributedIndex(gid, callback, options) {

}